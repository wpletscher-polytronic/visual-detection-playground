"""TODO: points -> Gaussian heatmap + sub-pixel offset + radius targets.
Write from the CenterNet equations. Do not copy gaussian_radius: the usual version has
the quadratic denominator as 2 instead of 2a. Decide sigma yourself (see PLAN.md)."""
